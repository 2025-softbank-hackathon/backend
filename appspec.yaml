version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: chatapp-dev-task-blue:2
        LoadBalancerInfo:
          ContainerName: chatapp-container
          ContainerPort: 3000